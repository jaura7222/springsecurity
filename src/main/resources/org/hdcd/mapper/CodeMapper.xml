<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hdcd.mapper.CodeMapper">
	
	
	<select id="getCodeGroupList" resultType="org.hdcd.domain.CodeLabelValue" >
		select group_code as value
			,group_name as label
		from code_group
		where use_yn = 'Y'
		order by group_name
	</select> 
	
	<select id="getCodeList" resultType="org.hdcd.domain.CodeLabelValue" >
		select code_value as value
			, code_name as label
		from code_detail
		where group_code = #{groupCode}
		and use_yn = 'Y'
		order by sort_seq
	</select>
	
</mapper>